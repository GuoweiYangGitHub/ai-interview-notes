# 可运行示例

示例代码位于仓库根目录。按下列顺序运行：

1. `npm run example:01`：结构化输出
2. `npm run example:03`：工具调用与 ReAct 循环
3. `npm run example:05`：MCP 融入 Agent
4. `npm run example:06`：受限执行环境与沙箱方案
5. `npm run example:07`：规划、任务板与收敛循环

其他框架与平台案例位于 `cookbook/`，对应脚本见根目录 `package.json`。
