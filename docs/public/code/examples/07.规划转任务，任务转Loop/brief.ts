/**
 * @file 业务输入
 * @description 对齐课12 的 brief，收成一份短需求。
 */

/** 交给 Plan 阶段的原始需求。 */
export const BRIEF = `
规划一次 45 分钟的会议纪要复盘。参会：产品、研发、测试。
会前已有 3 条待办：补登录失败用例、冻结方案 B 接口、通知设计改文案。
还没决定排序规则，也没决定要不要会上当场指定负责人和日期。
目标：排出本周前 2 项，并写下选择理由。
`.trim()

/**
 * 待澄清用的预设答案，避免卡在真实 stdin。
 * key 用于和模型问题做粗匹配。
 */
export const PRESET_ANSWERS = {
  排序规则: '按风险高低，登录失败优先于文案',
  当场指定: '会上指定负责人，截止日期会后由项目经理补',
}
